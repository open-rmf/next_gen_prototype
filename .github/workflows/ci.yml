name: Next-Gen Prototype CI

on:
  push:
    branches: [ epic/next-gen-base ]
  pull_request:
    branches: [ epic/next-gen-base ]

jobs:
  ci-check:
    name: Colcon Overlay Build & Test Suite
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/open-rmf/ros2_rust_base:jazzy

    steps:
      - name: Checkout Prototype Repository
        uses: actions/checkout@v4
        with:
          path: next_gen_ws/src/next_gen_prototype

      - name: Build Prototype Packages (Overlay Workspace)
        working-directory: next_gen_ws
        run: |
          source /opt/ros/jazzy/setup.bash
          rosdep update
          rosdep install --from-paths . --ignore-src -yr --skip-keys="rclrs ament_cargo ament_python"
          colcon build --packages-up-to sp_demo_nav2_bringup rmf_nav2_traffic rmf_path_server_demo rmf_path_server_test
        shell: bash

      - name: Run Automated Verification Tests
        working-directory: next_gen_ws
        run: |
          source /opt/ros/jazzy/setup.bash
          source install/setup.bash
          colcon test --packages-select rmf_path_server_test rmf_reservation_tests --event-handlers console_direct+
          colcon test-result --all
        shell: bash
